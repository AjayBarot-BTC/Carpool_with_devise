<ol class="microposts">
  <% @users.each do |user| %>  
    <%= render user %>
    <%= link_to "abc", user, method: :delete, data: { confirm: "Are you sure? This will remove the post and it is non-reversible" } %>
 <% end %>
 </ol>